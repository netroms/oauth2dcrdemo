<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Oauth2demo"
        android:usesCleartextTraffic="true"
        tools:targetApi="m"
        tools:replace="android:allowBackup,android:fullBackupContent">
        
        <!-- Welcome screen - First launch -->
        <activity
            android:name=".ui.WelcomeActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- OAuth callback router - Handles all deep link callbacks from DHIS2 -->
        <!-- Routes enrollment (iat) and login (code) callbacks to appropriate handlers -->
        <activity
            android:name=".ui.OAuthCallbackActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="dhis2oauth"
                    android:host="oauth" />
            </intent-filter>
        </activity>
        
        <!-- Login screen -->
        <activity
            android:name=".ui.LoginActivity"
            android:exported="false" />
        
        <!-- Main screen - After successful login -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />
    </application>

</manifest>